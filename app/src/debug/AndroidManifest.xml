<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Shell-only destructive test hook; never merged into release/benchmark. -->
        <receiver android:name=".diagnostics.CrashTestReceiver"
            android:process=":vpn" android:exported="true"
            android:permission="android.permission.DUMP" />
    </application>
</manifest>
